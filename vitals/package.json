{
  "name": "portals-web-vitals",
  "private": true,
  "version": "0.1.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild ./src/index.js --bundle --minify --tree-shaking=true --platform=browser --outfile=dist/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.15.7"
  },
  "dependencies": {
    "web-vitals": "^3.1.0"
  }
}
